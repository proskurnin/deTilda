services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env.prod
    volumes:
      - ./logs:/app/logs
      - /var/lib/detilda/_workdir:/app/_workdir
    restart: unless-stopped
